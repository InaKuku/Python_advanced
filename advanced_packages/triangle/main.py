# Create a module for printing a triangle. You will receive an integer number which is the size of the
# triangle.

from advanced_packages.triangle.trian import print_triangle

n = int(input())

print_triangle(n)